package clone

import "strings"

const helpShort = "Clone repositories"

var helpLong = strings.TrimSpace(`
The 'clone' command clones the repositories matching the pattern from the
server into the target directory.
`)

const helpExamples = ""
